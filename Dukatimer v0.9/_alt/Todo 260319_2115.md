Befunde

------

-------

----

----

Teile der Phase-2-Architektur fehlen weiterhin real im Codebestand.
SetupApp ist nicht vorhanden.
SensorManager ist nicht vorhanden.
xInputQueue wird im v0.9-Code nicht verwendet; der einzige Treffer liegt in Grundlagen.md:49.

Die Review-.md-Dateien sind teilweise veraltet oder zu optimistisch.
ToDo.md bewertet mehrere Punkte als weiter fortgeschritten, als der Code tatsächlich ist, vor allem bei Display-Integration, Event-Routing und Exposure-Guard.
Architecture_Audit.md nennt den Build noch fehlerhaft; nach kleinem Fix ist der aktuelle Build jetzt wieder grün.

Minimal gefixt

Offensichtlicher Konstruktorfehler im AppManager.
In AppManager.cpp:3 bis AppManager.cpp:15 wurden vier App-Instanzen mit falscher Argumentzahl konstruiert. Das habe ich minimalinvasiv korrigiert.
Zusätzlich wird in AppManager.cpp:81 bis AppManager.cpp:83 der Rückgabewert von setMode jetzt ausgewertet statt ignoriert.

Build-Status danach.
Der aktuelle PlatformIO-Build für esp32-s3-devkitc-1 läuft wieder erfolgreich durch.
Das heißt: Der Stand ist derzeit kompilierbar, aber noch nicht architektonisch sauber oder funktional vollständig.

Abgleich mit den .md-Dateien

Erledigt

SystemContext existiert und nutzt bool-Snapshots mit Mutex-Schutz.
SystemContext.h:136 bis SystemContext.h:189
HardwareManager existiert.
PaperManager existiert.
StorageManager existiert.
IAppMode existiert.
AppManager existiert und routet Modi.
AppManager.cpp:25 bis AppManager.cpp:90
InputManager existiert und ist in main verdrahtet.
main.cpp:26 bis main.cpp:27, main.cpp:57 bis main.cpp:59
ExposureEngine existiert und ist in main verdrahtet.
main.cpp:26, main.cpp:57 bis main.cpp:58
DisplayManager existiert als Modul.
DisplayManager.h:1 und DisplayManager.cpp:1
Teilweise erledigt

AppManager Belichtungs-Guard.
Moduswechsel sind gesperrt, normale Eingaben nicht.
StorageManager Deferred Writing.
Dirty-Latching und Retry sind vorhanden, aber keine globale Exposure-Sperre.
RAII MutexGuard.
Im SystemContext vorhanden, im PaperManager noch nicht.
DisplayManager.
Implementiert, aber nicht im System aktiv.
Phase-2-Foto-Apps.
Es gibt BWDoseApp, SGDoseApp, BWFStopApp und ZoneModeApp, aber nicht die in Grundlagen.md genannten SetupApp- oder klassischen BWApp-/SGApp-Strukturen.
Offen

SetupApp
SensorManager
xInputQueue-basierter Event-Loop auf Core 1
vollständiger Eingabe-Guard während laufender Belichtung
echte End-to-End-Display-Integration in main
saubere Konsolidierung der ExposureEngine-Shutoff- und liveTime-Logik
const-Korrektheit in PaperManager-Lesemethoden
dokumentierte Flash-Schreibsperre während Belichtung
Aktueller Stand

Wir stehen auf einem wieder kompilierbaren Zwischenstand mit vorhandenem Fundament, aber noch ohne sauberen Kernelabschluss und ohne aktiv verdrahtete Anzeige.
Kurz gesagt:

Fundament vorhanden
Build grün
mehrere Module nur teilweise integriert
Dokumentation beschreibt den Stand teilweise besser als er real ist
Priorisierte offene Probleme

P0
Event-Guard während Belichtung fehlt im normalen Eingabepfad.
Risiko: Apps können während aktiver Belichtung weiter Events verarbeiten.

P0
StorageManager schreibt ohne globale Exposure-Sperre.
Risiko: Verstoß gegen das eigene Sicherheitsmodell und mögliche Störungen während Lichtemission.

P1
DisplayManager ist nicht in main integriert.
Risiko: Das Anzeige-Subsystem existiert nur auf dem Papier bzw. als toter Code.

P1
ExposureEngine liveTime und Shutoff-Pfad sind inkonsistent.
Risiko: falsche Telemetrie und potenziell unsaubere Echtzeit-/Hardware-Trennung.

P2
SetupApp und SensorManager fehlen vollständig.
Risiko: Phase-2-Roadmap ist funktional noch nicht abgeschlossen, auch wenn viele Basisklassen bereits da sind.